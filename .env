# Fixed ports for the replica. `npm run dev` / scripts/serve.sh read these.
FRONTEND_PORT=3037
BACKEND_PORT=8047

# Where the Next.js frontend proxies the inferred backend API to.
BACKEND_URL=http://localhost:8047
